/** Action Functions
 * Add new custom functions with:
 * const { yourFunction } = require('./yourFile')
 * module.exports = { move, yourFunction }
 */
const { move } = require('./move')

module.exports = { move }
